import RegisterPage from './components/RegisterPage';

export default{
    mode: 'history',

    routes: [
        {
            path: '/register',
            component: RegisterPage
        }
    ]
}